const Path = require("path");
const { playSound } = require("../helpers");

module.exports = {
	name: "isabel",
	aliases: [],
	execute: async message => message.reply("pascal ist ne geile Sau 🤯"),
};
